# User Git Rules

- Never create a commit without the user's explicit permission.
- Never add `Co-authored-by` or other co-author trailers to commits.
- Keep commit bodies short; omit the body when the subject is sufficient.
- When a repository has a pull request template under `.github/`, make the PR description follow that template.
